#include "occlusion.h"


OcclusionIntegrator::OcclusionIntegrator()
{
    handle = "Occlusion";
}

Vector3 OcclusionIntegrator::getPixelColor(Ray& ray,
    SceneManager &scene,
    Randomizer& randEngine,
    int rayDepth)
{
    // TODO

    return Vector3(0.0f);
}