#include "udpt.h"


UDPTIntegrator::UDPTIntegrator()
{
    handle = "UDPT";
}

Vector3 UDPTIntegrator::getPixelColor(Ray& ray,
    SceneManager &scene,
    Randomizer& randEngine,
    int rayDepth)
{
    // TODO

    return Vector3(0.0f);
}