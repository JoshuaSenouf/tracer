#ifndef QT_H
#define QT_H

#include <string>

#include <QtWidgets/QApplication>

#include "gui/qt/tracerwindow.h"
#include "utility/render.h"

#include "context/context.h"


struct ContextQt : public Context
{
    public:
        ContextQt();
        ContextQt(
            CLIArgs cli_args);

        virtual bool Initialize() override;
        virtual bool Execute() override;

    private:
        RenderGlobals globals_;
        TracerWindow *tracer_window;
        QApplication *qt_app;
};

#endif // QT_H
